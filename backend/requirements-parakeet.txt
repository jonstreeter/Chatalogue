# Optional NVIDIA Parakeet transcription engine support.
#
# Install inside the app venv after torch/torchaudio and backend/requirements.txt:
#   pip install -r backend/requirements-parakeet.txt
#
# Keep this pinned to the tested NeMo line; newer NeMo releases are not assumed
# to be safer on Windows/macOS without validation.

nemo_toolkit[asr]==2.7.0
