# Shared backend dependency entry point.
#
# Install torch/torchaudio from a platform-specific file first:
#   Windows RTX 50xx: pip install --pre -r backend/requirements-windows-cu128.txt
#   macOS / stable fallback: pip install -r backend/requirements-macos.txt
#
# Then install:
#   pip install -r backend/requirements.txt
#   pip install -r backend/requirements-dev.txt   # test/dev tools
#   pip install pyannote.audio==4.0.4 --no-deps
#   pip install -r backend/requirements-parakeet.txt   # optional

-r requirements-common.txt
-r requirements-pyannote.txt
