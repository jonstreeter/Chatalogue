# Handoff: <topic>

**Date:** YYYY-MM-DD
**Spec:** `docs/dev/specs/YYYY-MM-DD-<slug>-design.md` (or _(none)_)
**Plan:** `docs/dev/plans/YYYY-MM-DD-<slug>.md` (or _(none)_)

> Written when work stops mid-flight. The reader is a fresh session with no
> chat history — assume it knows nothing you have not written down here.
> `python scripts/check_sdd_docs.py status` surfaces this file automatically.

## Status

Where things actually stand. Not what was attempted — what is true now.

## Next step

The single next action, with the exact file path and function to open. One
step, not a plan; the plan lives in the plan doc.

## Verification state

What has been run and what it said. Name the command so it can be re-run.

- `<command>` → PASS / FAIL, notes.

## Blockers

What is in the way, and who or what has to resolve it. Include anything that
needs a decision from the owner.

## Diagnosis worth keeping

Any root cause found along the way, especially one that has not been fixed
yet. This is the section that stops the next session rediscovering it. If an
escalation tripwire fired and the answer was "patch it anyway", record that
decision here in one line.
