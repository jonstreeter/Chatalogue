# ADR-NNNN: <decision in a single line>

**Date:** YYYY-MM-DD
**Status:** Proposed
<!-- Proposed | Accepted | Superseded by ADR-NNNN -->
**Deciders:** <name>

> Only `Accepted` ADRs may be registered as a contract authority in
> `docs/dev/contracts.json`. The validator checks this.

---

## Context

What forces make this decision necessary? State the constraints that are real
(measured, imposed by a platform, required by the owner) separately from the
ones that are assumed. An ADR whose context is all assumption is a guess with
formatting.

## Decision

What we are doing, in the active voice. One paragraph.

## Alternatives considered

Two or three real options, each with the trade-off that made it lose. If there
was only ever one option, this is not an ADR — write it as a spec tenet instead.

1. **<option>** — pros / cons / why not chosen.
2. **<option>** — pros / cons / why not chosen.

## Consequences

- What becomes easier.
- What becomes harder, and what that costs later.
- What this forecloses.

## What would change our mind

The observation that should trigger revisiting this. Be concrete: a latency
number, a failure mode, a dependency going unmaintained. An ADR with no
falsifier never gets revisited, it just gets resented.

## Rollback

How to undo this if it turns out wrong, and what makes that expensive.
